{
  "version": 2,
  "builds": [
    {
      "src": "cybersuite-frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "^/(.*)$", "dest": "/cybersuite-frontend/dist/index.html" }
  ]
}
